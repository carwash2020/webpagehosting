{
  "name": "triplehenterprises-site-tests",
  "private": true,
  "description": "Automated tests and consistency checks for triplehenterprisesllc.biz. Dev-only tooling -- has no effect on the deployed static site, which still has zero build step.",
  "scripts": {
    "test": "node --test tests/parts-reference.test.js tests/sync-merge.test.js tests/data-layer.test.js tests/finance-split.test.js tests/theme-toggle.test.js tests/regression-checker.test.js tests/visual-snapshot.test.js tests/tools-light-mode.test.js tests/desktop-layout.test.js tests/desktop-sidebar.test.js",
    "check-visual-snapshot": "node scripts/check-visual-snapshot.js",
    "check-consistency": "node scripts/check-consistency.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
